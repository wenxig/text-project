const activityAddModel = {
    selectAllCase:
    'select * from ev_events',
    addCase:'select * from ev_events',
    activityDele:'select * from ev_article_cate where cate_name="科技" or cate_alias= "keji"',
   
}

module.exports = activityAddModel